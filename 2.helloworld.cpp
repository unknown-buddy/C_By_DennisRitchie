/*
 As said in the previous code that printf doen't include any new line unless we specify it
so,in this code there are two printf statements without any \n feed which results 
in the output on the same line.
*/
#include<stdio.h>
main()
{
	printf("hello");
	printf(",world");
}
